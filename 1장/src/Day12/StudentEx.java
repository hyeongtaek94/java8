package Day12;

public class StudentEx {
	Student studetn = new Student();	//맨앞 대문자
	Person person = new Person();		//맨뒤 ();부분 생성자라함
										//메인클래스는 실행용, 실행하는 클래스는 1개여야함
										//나머지 하나는 참고용인 라이버르러가 있다.
	
	/*													public class ClassName {
	 * 필드(field): 객체의 데이터가 저장되는 곳					//필드
	 * 															int fieldName;
	 * 생성자(constructor): 객체 생성 시 초기화 역할 담당		//생성자
	 * 															ClassName() {	}
	 * 메소드(method): 객체의 동작에 해당하는 실행 블록			//메소드
	 * 															void methodName() {	}	소괄호의 유무에따라
	 * 																					메소드냐 필드냐 알아볼수있음
	 * */													}

