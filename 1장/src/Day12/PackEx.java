package Day12;

import java.util.*;	//util 다음에 *를쓴다면 util에 모든것을 쓸수있음

public class PackEx {
	public static void main(String[] args) {
		Random rd = new Random();
		// 원칙은 java.util.Random rd = new java.util.Random();
		// 이렇게써야 정상인데 길어서 import(가져오기)로 대체
		// 길게 쓰면 위에 import는 없음.

	
//	시스템 패키지 종류
//	applet은 인터넷에서 가져오는거 지금은 안씀
	
		
//	classpass 윈도우즈 시스템의 환경변수 path는 DOS환경에서
//	커맨드가 들어 있는 폴더를 알려주는 역할을 한다.
//	환경변수 classpath는 자바 시스템에게 수행하고자
//	하는 클래스가 어느 폴더에 있는지 알려주는 역할을 한다
	
	}
}
